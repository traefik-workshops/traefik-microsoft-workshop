apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: traefik-dashboard
  namespace: traefik
spec:
  entryPoints:
    - web
  routes:
  - match: Host(`dashboard.traefik.4.249.137.41.sslip.io`)
    kind: Rule
    services:
    - name: api@internal
      kind: TraefikService